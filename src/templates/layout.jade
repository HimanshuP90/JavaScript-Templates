block config
    - var name = 'Himanshu'
    - var age = 28
    - var pageTitle = 'Homepage'

doctype
html(lang="en")
    head
        title #{pageTitle}
    body
        block main
        h1 Hello World